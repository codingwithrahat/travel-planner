spring.application.name=travel-planner

# Dynamic Port Binding (Render assigns $PORT, fallback to 8080)
server.port=${PORT:8080}

# MongoDB Connection (supports environment variable override on Render)
spring.mongodb.uri=${SPRING_DATA_MONGODB_URI:${SPRING_MONGODB_URI:mongodb+srv://codingwithrahat_db_user:BhAiX27uhGCarSwi@cluster0.wfionxn.mongodb.net/travelplanner?appName=Cluster0}}
